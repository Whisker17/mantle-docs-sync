# Mantle Network Docs (Nextra)

This project migrates `https://docs.mantle.xyz/network` into a local Nextra docs site.

## Commands

- `npm run migrate:mantle-network` fetch all sitemap pages and generate content
- `npm run dev` run local development server
- `npm run build` build production output
